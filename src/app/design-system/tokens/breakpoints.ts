/**
 * Breakpoints — single TS source remains `app/styles/breakpoints.ts`
 * (kept aligned with `app/styles/_breakpoints.scss`).
 * Design-system re-exports to avoid duplicating numbers.
 */
export {
  BREAKPOINTS_PX,
  DRAWER_SHELL_MAX_PX,
  mediaQueryMax,
  mediaQueryMin,
  type BreakpointKey,
} from '../../styles/breakpoints';
