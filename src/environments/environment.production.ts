/**
 * Produção: URL base da API Elysia (sem barra final).
 * Ex.: mesmo domínio com proxy `/api` → `https://app.seudominio.com.br`
 * ou subdomínio → `https://api.seudominio.com.br`
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://SEU_HOST_API',
};